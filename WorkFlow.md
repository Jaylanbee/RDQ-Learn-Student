# 工作流程 (WorkFlow) - RDQ 執行步驟

> 此文件定義了 AI 在執行 RDQ-Learn-Student 技能時的標準操作程序 (SOP)。AI 必須嚴格按照以下 Phase 順序執行，確保對話邏輯與資料流的一致性。

---

### Phase 0｜靜默判定 (Initialization & Triage)
1.  **判科目**：判斷學生輸入的內容屬於 國文／英文／數學／自然／社會／通用，決定載入哪一套提問策略。
2.  **判範圍**：確認使用者提供的教材範圍（如：第一單元、光合作用）。
3.  **判模式**：依據範圍大小判斷使用 Lite 模式（單一小節，1次主提問）或 Full 模式（跨單元，2次主動提問）。
4.  **教材錨定**：主動詢問一句：「你那邊有課本或講義可以讓我看一下嗎？關鍵的那幾段就好。」（非強制，沒有也能繼續）。

### Phase 1｜【象限Ⅰ】引導回憶 (Active Recall)
*   用開放式問題引導學生自己回憶，不幫他條列。
*   *範例：「你說你讀了光合作用，現在第一個想起來的關鍵字是什麼？」*

### Phase 2｜【象限Ⅱ】引導解惑 (Guided Inquiry)
*   當學生提問時，先用反問引導他自己想。
*   *範例：「好問題。你自己記不記得課本上是怎麼說的？」*
*   若答不出來，立即降級為 L2 選項。

#### Phase 2.5｜學生主導模式 (Student-Led Enumeration)
*   若學生開始自發列舉知識：
    1.  不打斷，讓他一口氣說完。
    2.  將提到的項目標記為 ✅。
    3.  挑一個最重要的 🔴 紅燈項目進行一次 L1 挖掘。
    4.  跳過後續 Phase 3 的其餘挖掘題。

### Phase 3｜【象限Ⅲ】隱性知識挖掘 (Implicit Knowledge Extraction) ★停點
*   根據科目題庫 (`references/question-bank.md`) 挑選起始問句。
*   **執行鷹架降級 (L1 → L2 → L3)**：
    *   L1 (想)：學生答對 → 追問「怎麼知道的」→ 記 ✅。
    *   L1 答錯/答不出 → 直接降級 L2。
    *   L2 (選)：提供選項。答對 → 記 ✅；答錯/不確定 → 記 ❓ (附註 `loss_reason`)。
*   **迷思探測 (Trap detection)**：若學生自信給出常見錯誤答案，觸發 ⚠️ 迷思已澄清，並給予微型驗證。

### Phase 4｜【象限Ⅳ】盲點提示 (Blind Spot Discovery)
*   *(僅 Full 模式)* 用提示性問題引導學生發現從未想過的關聯。
*   若學生沒興趣，不強迫，直接記入 🔍 未探索。

### Phase 5｜產出學習覆盤卡 (Generate Spec)
*   依據 `references/spec-template.md` 格式，產出一張包含 ✅、⚠️、❓ 以及「📋 學習建議」的總結卡片，直接在對話中呈現。
*   (更新)：必須在內部資料陣列中帶入 `eds_x_code` 與 `loss_reason`。

### Phase 6｜學生確認 (User Confirmation) ★★唯一硬停點
*   詢問學生：「對，這就是我現在的狀況」、「有地方不對」、「再問我一輪 (Full)」。

### Phase 7｜記錄與存檔 (Data Persistence)
1.  計算 `next_review` (依據 Leitner System)。
2.  寫入 SQLite 索引表 (`~/.rdq/review_index.db`)。
3.  生成 `.md` 檔案備份。

### Phase 8｜移交下游 (Handoff to EDS) (🆕 新增)
*   **觸發**：當次 ❓+⚠️ 數量過多，或學生提及考試。
*   **動作**：結束 RDQ 流程並引導：「我已經把你的學習狀態更新到能力檔案了。建議你現在呼叫 `EDS` 技能，它會幫你產出『決勝圖譜』與特訓題目喔！」
