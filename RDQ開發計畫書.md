# RDQ-Learn-Student 開發計畫書 (與 EDS 銜接篇)

## 一、 系統願景與戰略定位

在教育決策系統（EDS）的方法論中，目標是實現「第一志願 = 知識矩陣 × 命題決策 × 有效學習」。
在這個生態系中，**RDQ-Learn-Student** 與 **EDS** 扮演著互相依存、分工明確的上下游關係，形成一個完整的學習閉環 (Closed Loop)。

### 1. 戰略分工
| 系統 | 戰略定位 | 對應情境 | 核心目標 | 產出/結果 |
|---|---|---|---|---|
| **RDQ (上游)** | 探勘兵與檢傷分類 | 課後當天、平時小考 | 確保 100% 吸收、找出盲點 | `review_index.db` (個人能力檔案) |
| **EDS (下游)** | 重裝備部隊與實戰演練 | 考前衝刺、段考、會考 | 高 ROI 決策、考試滿分 | 決勝圖譜 (Decision Graph) |

### 2. RDQ 的本質：為 Engine 5 (Adaptive Engine) 提供燃料
EDS v1.0 文件中提到，要實現終極的 Adaptive Engine，最缺乏的是「目前能力」這個動態變數。
RDQ 的任務就是在最低壓力的情況下，透過引導式對話，測量出學生對各個知識點的掌握度（✅、❓、⚠️），並將這些資料作為燃料，寫入資料庫供 EDS 使用。

---

## 二、 雙系統資料銜接契約 (Data Handoff)

為了讓 RDQ 產生的資料能被 EDS 直接讀取與分析，我們必須對齊雙方的資料結構 (Schema)。

1.  **知識點代碼對齊 (SSOT)**：
    *   EDS 的核心是 Layer 1 的「知識矩陣」。
    *   RDQ 產出的覆盤卡 (`spec-template.md`) 必須新增 `eds_x_code` 欄位，盡可能對應 108 課綱的 X 軸代碼（例如 `Bc-Ⅳ-3`）。
2.  **失分/卡關原因標籤 (Loss Reason)**：
    *   對應 EDS Module B-2，當學生在 RDQ 答錯時，RDQ 需標記卡關原因。
    *   新增欄位：`loss_reason`（可選值：概念錯誤、計算錯誤、圖表判讀、推理不足、看錯題）。

---

## 三、 RDQ 具體優化計畫 (Phase 8 移交機制)

為確保使用者流暢地從日常學習過渡到考前衝刺，RDQ 將新增 **Phase 8 (移交下游)** 流程：

*   **觸發條件**：
    1.  對話中偵測到考試關鍵字（「快考試了」、「下週段考」）。
    2.  單次覆盤中，❓ (待確認) 與 ⚠️ (迷思) 的總數大於等於總題數的 50%。
*   **執行動作**：
    1.  溫和地婉拒長篇大論的解題要求（嚴守「不教書」底線）。
    2.  輸出移交引導語：「你今天的覆盤做得很棒！我已經把你的學習狀態更新到個人能力檔案了。既然你快要考試了（或遇到比較多卡關），建議你現在呼叫 `EDS` 技能，它會根據你目前的弱點，搭配歷屆考題投資報酬率 (ROI)，幫你產出『決勝圖譜』與特訓題目喔！」

---

## 四、 階段開發時程

*   **Phase A (Schema 升級)**：修改 `spec-template.md`，納入 EDS 所需之欄位。
*   **Phase B (Agent 邏輯升級)**：修改 `SKILL.md`，實作代碼映射原則、卡關原因分析以及 Phase 8 的移交邏輯。
*   **Phase C (整合測試)**：與現有 `review_index.db` SQLite 機制進行寫入整合，確保 EDS 能順利讀取。