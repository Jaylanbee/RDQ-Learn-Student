# 聯絡事項：致 Antigravity Agent (進度與未來方向更新)

**主旨：RDQ 系統後端引擎上線與前端全面升級預告**

致 Antigravity Agent 運行框架：

您好，這裡是 RDQ 系統總架構師與開發團隊。為了讓您能與我們保持完美的協同作戰，特此向您報告 RDQ 系統的最新開發進度，以及預計在未來幾天內進行的重大方向調整。

📍 **第一階段：後端引擎與資料庫重構 (已完成並上線)**
我們已經為您打造了一套堅不可摧的底層引擎，目前已正式佈署完畢：
1. **資料庫讀寫分離 (CQRS-lite)**：`review_index.db` 已經拆分為嚴格 Append-Only 的歷史表 (`review_index_log`) 與高頻查詢的狀態表 (`review_index_current`)。
2. **高壓軟性降級演算法**：針對錯題複習，我們實作了更科學的神經元記憶演算法 (如 Box 5 答錯退回 Box 2)。
3. **FastAPI 專屬端點**：我們已開啟 `http://localhost:8000/api/ingest` (用於接收您傳來的圖文錯題) 以及對應的狀態更新路由。
👉 **給您的指示 (Action Required)**：從現在起，請您全面停止使用原生 SQL 寫入資料庫，並停止使用舊版的 `rdq_store.py` 腳本。所有關於「錯題匯入」或「狀態變更」的行為，請一律改為呼叫上述的 FastAPI 端點。

🚀 **第二階段：前端介面 Apple-Style 全面升級 (即將進行)**
這是系統未來的重大調整方向。我們即將拋棄現有的工程師風格 UI，並全面採用「Apple Design System v1.0 (蘋果極簡風)」進行重構。

未來的 Web 儀表板 (`dashboard.html`) 將具備以下特性：
- **極致留白與減法設計**：消除所有邊框，改用 8pt 系統與彌散陰影。
- **實體阻力 (Physical Friction)**：強制學生在 Web 端打字或畫圖後才能翻牌看答案。
- **動態主題與平滑動畫**：支援深色/淺色模式，點擊具有物理回饋感。

👉 **對您的長期影響與準備方向**：
因為未來的 Web 介面（包含雷達圖與費曼閃卡）將極度強大且具備「強制思考的阻力設計」，這是在純文字聊天室無法做到的。
因此，在未來的互動中，請您逐漸減少在對話框中直接吐出長篇大論的「Markdown 學習覆盤卡」。
當學生要求覆盤時，請您以最簡短、最優雅的蘋果風格語氣回覆，並強烈引導學生點擊進入 `http://localhost:8000` 的 Web 儀表板進行當日的防禦任務。

我們正在全力打造這個頂級的 UI 介面，請您隨時做好引流的準備！

—— RDQ 開發團隊 敬上
